# One file for the whole repo. Patterns without a folder prefix apply to both
# projects; the sections below them are scoped to one.

node_modules/
dist/
*.log
.DS_Store

# assets/ -- Affinity writes one of these beside the document it has open
*.af~lock~

# Real notes identify real people -- never commit them, wherever they land.
.env
*.fixture.html
nfce-fixture.html
*.db
*.db-shm
*.db-wal
*.db-journal

# container/ -- the SQLite database and captured fixtures
container/data/

# mobile/ -- Expo caches and build output
mobile/.expo/
mobile/web-build/

# mobile/ -- native projects, when generated by `npx expo prebuild`
mobile/ios/
mobile/android/
