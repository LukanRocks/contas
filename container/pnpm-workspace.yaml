# Each package lives in its own folder here (backend, web, and future
# siblings). The mobile app is a separate project in ../mobile.
packages:
  - "*"

# better-sqlite3 ships a native binding; without this pnpm silently skips its
# build script and importing it fails at runtime.
onlyBuiltDependencies:
  - better-sqlite3
